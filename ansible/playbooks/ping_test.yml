- name: Ping Windows
  hosts: windows
  gather_facts: false
  tasks:
    - name: win ping
      ansible.windows.win_ping:

- name: Ping Linux
  hosts: linux
  gather_facts: false
  tasks:
    - name: ping
      ansible.builtin.ping:
